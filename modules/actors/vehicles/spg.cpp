#include "spg.h"

std::vector<bool> Spg::IsAvailableForShoot(const std::vector<Point>& points) {
    std::vector<bool> res;
    return res;
}
std::multimap<int, Point> Spg::GetAvailableMovePoints(Point target, int r) const {
    return {};
}
